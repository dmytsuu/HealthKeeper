= simple_form_for [message.conversation, message], method: :post do |f|
  = f.input :content, label: false
  = f.input :conversation_id, as: :hidden
  = f.button :submit, 'Submit', class: 'btn btn-primary btn-md'

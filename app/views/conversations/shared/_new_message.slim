= simple_form_for [message.conversation, message], method: :post, remote: true do |f|
  = f.input :content, required: true, disable: true, label: false
  = f.input :conversation_id, as: :hidden
  = f.submit 'Send', class: 'btn btn-primary btn-md'
